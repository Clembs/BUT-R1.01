with "gtkada";

project gigogne is

   for Main use ("gigogne.adb");
   
 
   package Compiler is
        for Default_Switches ("ada") use ("-g");
   end Compiler;

   package Pretty_Printer is
    for Default_Switches ("ada") use ("-nM");
   end Pretty_Printer;

end gigogne;

